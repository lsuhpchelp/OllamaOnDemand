# Export the module function if it exists
[[ $(type -t module) == "function" ]] && export -f module

# Find available port to run server on
port=$(find_port)
echo "Port: ${port}"
echo "Secret: ${secret}"

# Find available port to run Ollama server on
export port_ollama=$(find_port)
echo "Ollama Server Port: ${port_ollama}"
